# 前端完成 - 删除会话（软删除）

## 已完成的工作

### 1. ✅ API 配置

**文件**: `src/renderer/src/shared/config/api.ts`

```typescript
SESSION: {
  MY: '/api/session/my',
  PRIVATE: '/api/session/private',
  GROUP: '/api/session/group',
  MESSAGES: (sessionId: string) => `/api/session/${sessionId}/messages`,
  READ: (sessionId: string) => `/api/session/${sessionId}/read`,
  DELETE: (sessionId: string) => `/api/session/${sessionId}`  // ✅ 新增
}
```

### 2. ✅ API 方法

**文件**: `src/renderer/src/shared/api/session.ts`

```typescript
// 删除会话（软删除）
deleteSession(sessionId: string) {
  return request.delete(API_ENDPOINTS.SESSION.DELETE(sessionId))
}
```

### 3. ✅ Store 逻辑

**文件**: `src/renderer/src/entities/session/model/useSessionStore.ts`

```typescript
const deleteSession = async (sessionId: string) => {
  try {
    console.log('🗑️ 删除会话:', sessionId)
    
    // 调用后端 API 删除会话（软删除）
    await sessionApi.deleteSession(sessionId)
    console.log('✅ 后端删除成功')
    
    // 如果是当前活动会话，先清除选中状态
    if (activeSessionId.value === sessionId) {
      activeSessionId.value = null
    }
    
    // 从本地列表中移除
    sessions.value = sessions.value.filter((s) => s.id !== sessionId)
    
    console.log('✅ 会话已删除')
    return { success: true }
  } catch (error) {
    console.error('❌ 删除会话失败:', error)
    return { success: false, error: String(error) }
  }
}
```

### 4. ✅ UI 交互

**文件**: `src/renderer/src/widgets/sessionList/SessionList.vue`

- 删除按钮（悬停显示）
- 确认对话框
- 错误提示

## 前端调用流程

```
用户点击删除按钮
    ↓
弹出确认对话框
    ↓
用户确认
    ↓
调用 sessionStore.deleteSession(sessionId)
    ↓
调用 sessionApi.deleteSession(sessionId)
    ↓
发送 DELETE /api/session/{sessionId}
    ↓
后端处理（删除 session_user 记录）
    ↓
返回成功
    ↓
前端从列表中移除会话
    ↓
完成
```

## 后端需要实现

### 接口定义

**方法**: `DELETE`  
**路径**: `/api/session/{sessionId}`  
**功能**: 删除当前用户的 session_user 记录（软删除）

### 最简实现

```java
@DeleteMapping("/{sessionId}")
public Result<Void> deleteSession(@PathVariable String sessionId) {
    String userId = SecurityUtils.getCurrentUserId();
    
    // 删除当前用户的 session_user 记录
    sessionUserRepository.deleteBySessionIdAndUserId(sessionId, userId);
    
    return Result.success();
}
```

### 完整实现（推荐）

```java
@DeleteMapping("/{sessionId}")
public Result<Void> deleteSession(@PathVariable String sessionId) {
    String userId = SecurityUtils.getCurrentUserId();
    
    // 查找并验证
    SessionUser sessionUser = sessionUserRepository
        .findBySessionIdAndUserId(sessionId, userId)
        .orElseThrow(() -> new RuntimeException("会话不存在或无权访问"));
    
    // 删除记录
    sessionUserRepository.delete(sessionUser);
    
    return Result.success();
}
```

## 测试方法

### 前端测试

1. 鼠标悬停在会话上，显示删除按钮
2. 点击删除按钮，弹出确认对话框
3. 点击"确定"
4. 查看控制台日志：
   ```
   🗑️ 删除会话: 12743e2b142fc6dc8f8642bf928cc636
   ✅ 后端删除成功
   ✅ 会话已删除
   ```
5. 会话从列表中消失

### 后端测试

**如果后端未实现**:
```
🗑️ 删除会话: 12743e2b142fc6dc8f8642bf928cc636
❌ 删除会话失败: Error: Request failed with status code 404
```

**如果后端已实现**:
```
🗑️ 删除会话: 12743e2b142fc6dc8f8642bf928cc636
✅ 后端删除成功
✅ 会话已删除
```

刷新页面后，会话不再出现。

## 软删除的优势

1. **不影响其他用户** - 对方仍能看到会话和消息
2. **可以恢复** - 如果需要，可以添加恢复功能
3. **符合用户预期** - 用户认为只是删除自己这边的会话
4. **实现简单** - 只需删除一条数据库记录

## 相关文档

- [后端实现指南-删除会话.md](./后端实现指南-删除会话.md) - 详细的后端实现指南
- [功能说明-删除会话.md](./功能说明-删除会话.md) - 功能说明和UI设计
- [最新改进总结.md](./最新改进总结.md) - 所有最新改进的总结

## 总结

**前端状态**: ✅ 已完成，等待后端实现

**后端任务**: 
1. 实现 `DELETE /api/session/{sessionId}` 接口
2. 删除 `session_user` 表中的记录
3. 返回成功响应

**预计工作量**: 10-20分钟（非常简单）

**优先级**: 中（功能已可用，但刷新后会恢复）

---

**文档时间**: 2024-02-27  
**状态**: ✅ 前端已完成  
**等待**: 后端实现
