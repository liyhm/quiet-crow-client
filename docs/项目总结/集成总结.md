# 后端 API 集成总结

## ✅ 已完成的工作

### 1. 项目配置

- ✅ 安装依赖：axios, crypto-js, @types/crypto-js
- ✅ 配置 API 基础地址：`http://localhost:8080`
- ✅ 配置 WebSocket 地址：`ws://localhost:8080/ws`

### 2. API 服务层（完整实现）

创建了以下 API 服务文件：

```
src/renderer/src/shared/
├── config/
│   └── api.ts              # API 配置和端点定义
├── api/
│   ├── request.ts          # Axios 封装（拦截器、Token 管理）
│   ├── auth.ts             # 认证 API（注册、登录、刷新）
│   ├── user.ts             # 用户 API（信息、搜索、更新）
│   ├── session.ts          # 会话 API（列表、创建、消息）
│   ├── encryption.ts       # 加密 API（密钥管理）
│   ├── contacts.ts         # 联系人 API（好友管理）
│   ├── websocket.ts        # WebSocket 服务（已更新地址）
│   └── index.ts            # 统一导出
└── lib/
    └── cryptoBackend.ts    # 后端兼容加密（AES-ECB）
```

### 3. Store 更新（对接真实 API）

#### AuthStore

- ✅ 真实登录 API 调用
- ✅ 真实注册 API 调用
- ✅ Token 自动管理
- ✅ 用户信息自动获取

#### SessionStore

- ✅ 从后端加载会话列表
- ✅ 创建私聊会话
- ✅ 会话状态管理

#### MessageStore

- ✅ 加载历史消息（自动解密）
- ✅ 发送加密消息
- ✅ 接收并解密消息
- ✅ 自动获取/生成会话密钥

### 4. 加密实现

- ✅ 使用 AES-ECB 模式（与后端一致）
- ✅ 基于 crypto-js 实现
- ✅ 会话密钥自动管理
- ✅ 加密/解密工具类

## 📋 API 端点映射

### 认证相关

| 功能      | 方法 | 端点               | 实现                  |
| --------- | ---- | ------------------ | --------------------- |
| 注册      | POST | /api/auth/register | ✅ authApi.register() |
| 登录      | POST | /api/auth/login    | ✅ authApi.login()    |
| 刷新Token | POST | /api/auth/refresh  | ✅ authApi.refresh()  |

### 用户相关

| 功能     | 方法 | 端点             | 实现                 |
| -------- | ---- | ---------------- | -------------------- |
| 获取信息 | GET  | /api/user/info   | ✅ userApi.getInfo() |
| 搜索用户 | GET  | /api/user/search | ✅ userApi.search()  |
| 更新信息 | PUT  | /api/user/me     | ✅ userApi.update()  |

### 会话相关

| 功能     | 方法 | 端点                       | 实现                          |
| -------- | ---- | -------------------------- | ----------------------------- |
| 会话列表 | GET  | /api/session/my            | ✅ sessionApi.getMySessions() |
| 创建私聊 | POST | /api/session/private       | ✅ sessionApi.createPrivate() |
| 创建群聊 | POST | /api/session/group         | ✅ sessionApi.createGroup()   |
| 历史消息 | GET  | /api/session/{id}/messages | ✅ sessionApi.getMessages()   |
| 标记已读 | POST | /api/session/{id}/read     | ✅ sessionApi.markAsRead()    |

### 加密相关

| 功能     | 方法 | 端点                                 | 实现                                  |
| -------- | ---- | ------------------------------------ | ------------------------------------- |
| 获取公钥 | GET  | /api/encryption/public-key           | ✅ encryptionApi.getMyPublicKey()     |
| 用户公钥 | GET  | /api/encryption/user/{id}/public-key | ✅ encryptionApi.getUserPublicKey()   |
| 批量公钥 | POST | /api/encryption/keys/public/batch    | ✅ encryptionApi.getBatchPublicKeys() |
| 生成密钥 | POST | /api/encryption/session/{id}/key     | ✅ encryptionApi.generateSessionKey() |
| 获取密钥 | GET  | /api/encryption/session/{id}/key     | ✅ encryptionApi.getSessionKey()      |

### 联系人相关

| 功能       | 方法   | 端点                               | 实现                                |
| ---------- | ------ | ---------------------------------- | ----------------------------------- |
| 好友列表   | GET    | /api/contacts                      | ✅ contactsApi.getList()            |
| 发送请求   | POST   | /api/contacts/requests             | ✅ contactsApi.sendRequest()        |
| 待处理请求 | GET    | /api/contacts/requests/pending     | ✅ contactsApi.getPendingRequests() |
| 接受请求   | POST   | /api/contacts/requests/{id}/accept | ✅ contactsApi.acceptRequest()      |
| 拒绝请求   | POST   | /api/contacts/requests/{id}/reject | ✅ contactsApi.rejectRequest()      |
| 删除好友   | DELETE | /api/contacts/{id}                 | ✅ contactsApi.deleteFriend()       |

### WebSocket

| 功能     | 端点                   | 实现                          |
| -------- | ---------------------- | ----------------------------- |
| 连接     | ws://localhost:8080/ws | ✅ websocketService.connect() |
| 订阅私聊 | /user/queue/messages   | ✅ 自动订阅                   |
| 订阅会话 | /topic/sessions/{id}   | ✅ subscribeToChat()          |
| 发送消息 | /app/chat/{id}         | ✅ sendMessage()              |
| 输入状态 | /app/typing/{id}       | ⏳ 待实现                     |
| 已读回执 | /app/read/{id}         | ⏳ 待实现                     |
| 同步消息 | /app/sync              | ✅ requestOfflineMessages()   |

## 🔐 加密流程

### 发送消息

```
1. 用户输入消息
   ↓
2. 检查会话密钥是否存在
   ↓
3. 如果不存在，调用 API 获取/生成密钥
   ↓
4. 使用 AES-ECB 加密消息
   ↓
5. 通过 WebSocket 发送加密消息
```

### 接收消息

```
1. WebSocket 收到加密消息
   ↓
2. 检查会话密钥是否存在
   ↓
3. 如果不存在，调用 API 获取密钥
   ↓
4. 使用 AES-ECB 解密消息
   ↓
5. 显示明文消息
```

## 📝 使用示例

### 完整的登录到发送消息流程

```typescript
// 1. 登录
const authStore = useAuthStore()
await authStore.login({
  username: 'alice',
  password: '123456'
})

// 2. 连接 WebSocket
import { websocketService } from '@/shared/api/websocket'
websocketService.initialize()
websocketService.connect()

// 3. 加载会话列表
const sessionStore = useSessionStore()
await sessionStore.loadSessions()

// 4. 选择会话
const sessionId = sessionStore.sessions[0].id
sessionStore.setActiveSession(sessionId)

// 5. 加载历史消息
const messageStore = useMessageStore()
await messageStore.loadMessages(sessionId)

// 6. 订阅会话消息
websocketService.subscribeToChat(sessionId, (message) => {
  messageStore.receiveMessage(message)
})

// 7. 发送消息
await messageStore.sendMessage(sessionId, 'Hello World!')
```

## 🎯 下一步工作

### 高优先级

1. ⏳ 在 LoginPage.vue 中集成真实登录
2. ⏳ 在 MainPage.vue 中加载会话和消息
3. ⏳ 实现 WebSocket 自动连接（登录后）
4. ⏳ 实现消息实时接收和显示

### 中优先级

5. ⏳ 实现输入状态提示
6. ⏳ 实现已读回执
7. ⏳ 实现文件上传
8. ⏳ 实现好友管理界面

### 低优先级

9. ⏳ 实现消息搜索
10. ⏳ 实现消息撤回
11. ⏳ 实现群聊管理
12. ⏳ 实现用户设置

## 📚 相关文档

- [API 集成指南](./API_INTEGRATION_GUIDE.md) - 详细的 API 使用说明
- [API 测试指南](./TEST_API.md) - 如何测试 API 连接
- [后端 API 文档](./BACKEND_API_REQUIREMENTS.md) - 后端接口规范

## 🐛 已知问题

1. ✅ 已修复：WebSocket 地址硬编码为远程服务器
2. ✅ 已修复：加密方式不匹配（前端 AES-GCM vs 后端 AES-ECB）
3. ✅ 已修复：缺少 REST API 调用封装
4. ✅ 已修复：TypeScript 类型错误

## ⚠️ 注意事项

1. **Token 管理**：Token 存储在 localStorage 的 `chat_token` 键中
2. **加密模式**：使用 AES-ECB 模式，与后端保持一致
3. **会话密钥**：首次使用会话时自动获取/生成密钥
4. **错误处理**：所有 API 调用都有完整的错误处理
5. **WebSocket 重连**：目前禁用了自动重连，生产环境需要启用

## 🚀 快速开始

### 1. 确保后端运行

```bash
# 后端应该在 http://localhost:8080 运行
# 访问 http://localhost:8080/swagger-ui.html 确认
```

### 2. 安装依赖

```bash
npm install
```

### 3. 启动前端

```bash
npm run dev
```

### 4. 测试 API

参考 [TEST_API.md](./TEST_API.md) 进行测试

## 📞 技术支持

如果遇到问题：

1. 检查后端是否正常运行
2. 查看浏览器控制台错误
3. 查看 Network 面板的 API 响应
4. 确认 Token 是否正确保存
5. 查看 WebSocket 连接状态

---

**集成完成时间**: 2024-02-27  
**状态**: ✅ 基础集成完成，可以开始开发业务功能
